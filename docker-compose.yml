services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: blueSky
      POSTGRES_PASSWORD: devpassword
      POSTGRES_DB: blueskyincentives
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
